const Hero = () => {
    return (
        <div className="hero">
            <button onClick={() => alert("Explore our collection!")}>
                KOLLA IN VÅRA LEKSAKER!
            </button>
        </div>
    );
};

export default Hero;
